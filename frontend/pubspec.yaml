name: hertale
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.1
  cloud_firestore: ^6.0.0
  flutter_riverpod: ^2.5.1
  firebase_storage: ^13.0.0
  image_picker: ^1.2.0
  file_picker: ^10.3.2
  provider: ^6.1.5
  flutter_svg: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/assets/HerTale.png
    - lib/assets/empty cart.png
    - lib/assets/empty_pot.png
